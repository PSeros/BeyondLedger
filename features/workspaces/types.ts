// A workspace = a bank account (Phase 14). Shared by the Topbar switcher, the Settings manager, the
// form pickers, and the filters. Mirrors TagOption (id + name + color).
export type WorkspaceOption = {id: number; name: string; color: string};
